//. 変数「x」に6を代入します。
let x = 6;
//2. 変数「y」に8を代入します。
let y = 8;
//3. 変数「x」と変数「y」の平均に3を掛けた値を変数「a」に代入します。
let a = ((x + y) / 2) * 3;
//4. 変数「x」と変数「y」の積に5を加えた値を変数「b」に代入します。
let b = x * y + 5;
//5. 変数「a」と変数「b」の和を、変数「c」に代入します。
let c = a + b;
//6. 「a」「b」「c」それぞれの値を『Console(コンソール)』に出力しましょう。
console.log("a =" + a);
console.log("b =" + b);
console.log("c =" + c);
//「javascript_mission_3_1_2.js」ファイルを作成して解答してください。
